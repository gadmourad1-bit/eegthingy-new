# GaugeQuotient Gate 1

This is a separate exploratory opened-development gate, not a row in the
common-grid v6 leaderboard. The candidate achieved 75.0704% equal-dataset
balanced accuracy versus 74.3988% for its primary reference and won 11/17
strict subject comparisons. It nevertheless failed the frozen gate because
only two of five dataset deltas were nonnegative; the rule required at least
three. The prespecified action was to kill the candidate before disjoint Gate
2, so Gate 2 is unrun.

`plan.json`/`plan.sha256` and `analysis.json`/`analysis.sha256` are the imported
sealed pairs. This is not confirmation, SOTA evidence, or clinical evidence.
